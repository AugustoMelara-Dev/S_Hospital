# =============================================================================
# Nginx production config for S_Hospital.
#
# HTTP on port 80 always serves the app. When the optional SSL block is
# uncommented, requests to https://<server>/ are also served. Generate the
# cert materials with scripts/generate_local_ca.ps1.
#
# IMPORTANT: keep in sync with the .env SERVER_IP / APP_PORT.
# =============================================================================

# ---- HTTP (always on) ----
server {
    listen 80 default_server;
    server_name _;

    root /var/www/html/public;
    index index.html;

    client_max_body_size 32M;

    add_header X-Frame-Options "DENY" always;
    add_header X-Content-Type-Options "nosniff" always;
    add_header Referrer-Policy "same-origin" always;
    add_header Cross-Origin-Opener-Policy "same-origin" always;
    add_header Cross-Origin-Resource-Policy "same-origin" always;
    # Enforced CSP is set by Laravel middleware (AddSecurityHeaders) with a
    # per-request nonce. The browser-side CSP report endpoint is
    # /api/system/csp-report.
    add_header Content-Security-Policy-Report-Only "default-src 'self'; script-src 'self' 'nonce-{NONCE}'; style-src 'self' 'nonce-{NONCE}'; img-src 'self' data:; connect-src 'self'; frame-ancestors 'none'; report-uri /api/system/csp-report" always;

    gzip on;
    gzip_comp_level 6;
    gzip_min_length 1024;
    gzip_proxied any;
    gzip_vary on;
    gzip_types
        text/plain
        text/css
        text/javascript
        application/json
        application/javascript
        application/xml
        application/xml+rss
        application/wasm
        font/woff2
        image/svg+xml;

    # Health check: servir texto plano sin pasar por Laravel.
    location = /up { return 200 "ok\n"; add_header Content-Type text/plain; }

    # Cabeceras comunes de seguridad en todas las respuestas.
    add_header X-Frame-Options "DENY" always;
    add_header X-Content-Type-Options "nosniff" always;
    add_header Referrer-Policy "same-origin" always;
    add_header Cross-Origin-Opener-Policy "same-origin" always;
    add_header Cross-Origin-Resource-Policy "same-origin" always;
    # Enforced CSP es responsabilidad de AddSecurityHeaders (Laravel) con nonce
    # por peticion. Aqui dejamos el bloque Report-Only para que el navegador
    # envie violaciones a /api/system/csp-report sin bloquear renderizacion.
    # No lleva nonce: el Report-Only debe ser mas permisivo para ver
    # exactamente que bloquearia la enforced CSP.
    add_header Content-Security-Policy-Report-Only "default-src 'self'; script-src 'self' 'unsafe-inline' 'unsafe-eval'; style-src 'self' 'unsafe-inline'; img-src 'self' data:; connect-src 'self'; frame-ancestors 'none'; report-uri /api/system/csp-report" always;

    # Rutas API: enrutar a Laravel (PHP-FPM) antes que el fallback SPA.
    # IMPORTANTE: el orden es clave. /api/ debe ir ANTES de / para que
    # nginx no sirva el index.html de la SPA en lugar del backend.
    location /api/ {
        try_files $uri /index.php?$query_string;
        fastcgi_pass backend:9000;
        fastcgi_index index.php;
        include fastcgi_params;
        fastcgi_param SCRIPT_FILENAME $document_root/index.php;
        fastcgi_param PATH_INFO $query_string;
        fastcgi_read_timeout 600s;
        fastcgi_buffers 16 32k;
        fastcgi_buffer_size 64k;
    }

    # Sanctum CSRF cookie: tambien va al backend.
    location = /sanctum/csrf-cookie {
        try_files $uri /index.php?$query_string;
        fastcgi_pass backend:9000;
        fastcgi_index index.php;
        include fastcgi_params;
        fastcgi_param SCRIPT_FILENAME $document_root/index.php;
    }

    # Denegar acceso a archivos/dotfiles sensibles.
    location ~ /\.(?!well-known).* {
        deny all;
        return 404;
    }

    # Cache agresivo para assets con hash (vite genera nombres tipo index-XXX.js).
    location ~* \.(?:css|js|mjs|woff2?|ttf|otf|eot|svg|png|jpe?g|gif|webp|ico|map)$ {
        try_files $uri =404;
        expires 7d;
        add_header Cache-Control "public, max-age=604800, immutable";
        access_log off;
    }

    # PHP-FPM directo (por si se accede a /index.php).
    location = /index.php {
        try_files $uri =404;
        fastcgi_pass backend:9000;
        fastcgi_index index.php;
        include fastcgi_params;
        fastcgi_param SCRIPT_FILENAME $document_root/index.php;
        fastcgi_read_timeout 600s;
        fastcgi_buffers 16 32k;
        fastcgi_buffer_size 64k;
    }

    # SPA fallback: cualquier otra ruta devuelve el index.html para que
    # React Router tome el control. Va DESPUES de /api/ y /sanctum/.
    location / {
        try_files $uri $uri/ /index.html;
    }
}

# ---- HTTPS (optional, uncomment after running generate_local_ca.ps1) ----
# server {
#     listen 443 ssl http2;
#     server_name _;
#
#     ssl_certificate     /etc/nginx/ssl/hospital-server.crt.pem;
#     ssl_certificate_key /etc/nginx/ssl/hospital-server.key.pem;
#     ssl_protocols       TLSv1.2 TLSv1.3;
#     ssl_ciphers         HIGH:!aNULL:!MD5;
#     ssl_prefer_server_ciphers on;
#     ssl_session_cache   shared:SSL:10m;
#     ssl_session_timeout 1d;
#
#     add_header Strict-Transport-Security "max-age=31536000; includeSubDomains" always;
#     add_header X-Frame-Options "DENY" always;
#     add_header X-Content-Type-Options "nosniff" always;
#     add_header Referrer-Policy "same-origin" always;
#
#     # Same root, same blocks as HTTP server. To avoid duplication,
#     # move common location blocks into an /etc/nginx/snippets/hospital.conf
#     # file and `include` it from both servers.
#     root /var/www/html/public;
#     index index.html;
#     client_max_body_size 32M;
#     # ... copy location blocks from the HTTP server above ...
# }
