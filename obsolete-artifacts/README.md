# Obsolete release artifacts

Estos archivos se conservan solo como referencia historica. No entregar al hospital.

El paquete vigente debe generarse desde `offline-release/` con `scripts\make_offline_release.ps1` y validarse con `scripts\assert_offline_release_clean.ps1`.
