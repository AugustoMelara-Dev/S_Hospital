import { CalendarOutlined, DatabaseOutlined, ReloadOutlined } from '@ant-design/icons';
import { Col, Descriptions, Row } from 'antd';
import { formatDateLong, formatLocalizedDateTime } from '@/lib/format/formatDate';
type Props = { ariaLabel: string; from: string; to: string; source: string; updatedAt?: number | string | null };
export function ReportScope({ ariaLabel, from, source, to, updatedAt }: Props) { const updated = updatedAt ? formatLocalizedDateTime(typeof updatedAt === 'number' ? new Date(updatedAt) : updatedAt) : 'Al cargar este reporte'; return <section aria-label={ariaLabel} className="border border-border p-3"><Row gutter={12}><Col xs={24} md={8}><Descriptions size="small" column={1} items={[{ key: 'period', label: <><CalendarOutlined /> Período</>, children: `${formatDateLong(`${from}T12:00:00`)} al ${formatDateLong(`${to}T12:00:00`)}` }]} /></Col><Col xs={24} md={8}><Descriptions size="small" column={1} items={[{ key: 'source', label: <><DatabaseOutlined /> Alcance</>, children: source }]} /></Col><Col xs={24} md={8}><Descriptions size="small" column={1} items={[{ key: 'updated', label: <><ReloadOutlined /> Última actualización</>, children: updated }]} /></Col></Row></section>; }
