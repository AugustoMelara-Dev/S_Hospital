# Dejar vacio para que el build use /api y /sanctum en el mismo host LAN.
VITE_API_BASE_URL=

# Solo desarrollo Vite: el contenedor frontend resuelve el backend por nombre de servicio.
VITE_DEV_API_PROXY_TARGET=http://localhost:8000
