# Registro reproducible — 2026-07-21

| Comando | Exit | Resultado |
|---|---:|---|
| `docker compose exec -T backend php artisan test --compact --no-ansi` | 0 | 953 passed, 7133 assertions, 13 skipped, 846.97 s |
| `docker compose exec -T frontend npm run typecheck` | 0 | TypeScript sin errores |
| `docker compose exec -T frontend npm run lint` | 0 | ESLint sin errores |
| `docker compose exec -T frontend npm run check:ui-legacy:final` | 0 | 376 archivos, 0 violaciones |
| `docker compose exec -T frontend npm run check:ui-rules` | 0 | 380 archivos conformes |
| `docker compose exec -T frontend npm run test:segmented` | 1 | 1085 passed, 1 failed; detectó expectativa antigua `Caja #12` |
| `docker compose exec -T frontend npm run test -- --run src/shell/InstitutionalShell.test.tsx src/shell/status/ContextBar.test.tsx` | 0 | 27/27 tras corregir la regresión |
| `docker compose exec -T backend php artisan test --compact --no-ansi tests/Unit/Support/MoneyTest.php` (RED) | 1 | prueba detectó API monetaria pública con float |
| mismo comando tras implementación (GREEN) | 0 | 17 passed, 40 assertions |
| `npm audit --omit=dev --json` (antes) | 1 | 1 alta, 3 moderadas |
| `npm audit --omit=dev --json` (después) | 0 | 0 vulnerabilidades runtime |
| `corepack pnpm install --frozen-lockfile --lockfile-only --ignore-scripts` | 0 | lockfile pnpm reproducible con pnpm 11.7.0 |
| `powershell -File scripts/security/supply-chain-check.ps1 -ProjectRoot .` | 0 | 0 findings, 0 warnings |
| `npx --no-install shadcn info --json` | 0 | Vite/Tailwind 4/radix-nova/Lucide; 40 componentes locales |
| `powershell -File scripts/docker_compose_dev_contract.test.ps1` (RED) | 1 | detectó healthcheck sin límite propio de curl |
| mismo comando tras implementación (GREEN) | 0 | probes acotados a 4 s; contrato reproducible intacto |
| `docker compose exec -T frontend npm run test -- --run src/test/playwright-real-config.test.ts` (RED) | 1 | runner real ignoraba Chromium offline configurado |
| mismo comando tras implementación (GREEN) | 0 | 1/1; `executablePath` y `--no-sandbox` configurables |

El primer smoke real desde Docker no abrió el navegador porque la configuración no consumía `PLAYWRIGHT_CHROMIUM_EXECUTABLE_PATH`; el intento desde Windows coincidió con un bloqueo temporal del servidor de desarrollo durante PHPStan. Ninguno se cuenta como prueba funcional. La repetición posterior al arreglo se registra únicamente cuando finalice con código 0. La suite frontend segmentada completa también debe repetirse antes de marcarla PASS.

## Repetición final

Los dos pendientes de la línea base fueron cerrados:

- `docker compose exec -T frontend npm run test:segmented`: código 0; 144 archivos, 1.093 tests aprobados, 0 fallidos, 0 omitidos, 12/12 segmentos, 988,7 s tras incorporar la regresión accesible de rutas denegadas.
- `npm run test:e2e:release` contra el compose de producción, Nginx, PHP-FPM y MariaDB aislada: código 0; 2/2 escenarios reales, sin errores de consola.

El registro completo, incluida la persistencia SQL y el restore, está en `release-certification-2026-07-21.md`.
