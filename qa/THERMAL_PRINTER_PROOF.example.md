# Legacy receipt proof

This file is kept only as a historical PR #4 compatibility artifact. Current
production validation uses:

```text
qa/INSTITUTIONAL_RECEIPT_PRINT_PROOF.example.md
qa/INSTITUTIONAL_RECEIPT_PRINT_PROOF.md
```

Do not create the deprecated printer proof for final handoff. The active
preflight requires institutional receipt evidence for media carta, carta and
A5, plus reprint and browser print settings.
