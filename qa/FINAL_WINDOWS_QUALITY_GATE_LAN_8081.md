# Final Windows quality gate LAN 8081

## Resultado

- Estado: PASS
- Fecha/hora local: 2026-06-16 22:51
- Comando canonico final: `powershell -NoProfile -ExecutionPolicy Bypass -File scripts\quality_gate_windows.ps1 -Full`
- Entorno: Windows, proyecto `C:\Projects\S_Hospital`

## Checks incluidos

- `git diff --check`: PASS
- Backend focal `InvoiceDialysisPrescriptionTest`: PASS, 5 tests
- Backend focal `CashPaymentsReceiptTest`: PASS, 27 tests
- Backend focal `InstitutionalReceiptPaymentIntegrationTest`: PASS, 8 tests
- Backend focal `InstitutionalReceiptPdfTest`: PASS, 9 tests
- Backend focal `BackupWorkflowTest`: PASS, 22 tests
- Backend focal `AuthorizationStrategyTest`: PASS, 2 tests
- Backend completo `php artisan test --colors=never`: PASS, 618 tests, 4084 assertions, 11 skipped
- Backend `vendor\bin\pint --test`: PASS
- Backend `vendor\bin\phpstan analyse --memory-limit=1G --no-progress`: PASS, no errors
- Frontend `npm.cmd run test:critical`: PASS, 57 tests
- Frontend completo `npm.cmd run test:full:windows`: PASS, 68 files, 309 tests
- Frontend `npm.cmd run typecheck`: PASS
- Frontend `npm.cmd run lint`: PASS
- Frontend `npm.cmd run build`: PASS
- Release E2E `npm.cmd run e2e`: PASS, 1 Playwright release test

## Resultado final del script

`WINDOWS_QUALITY_GATE_PASSED`

## Notas

- Antes de pasar, el gate detecto fallos reales en Pint, PHPStan, full backend y full frontend.
- Se corrigieron imports, propiedades dinamicas de aggregates, nullsafe innecesario, metodo muerto, pruebas de decrypt para backups chunked y expectativa de navegacion de Recibos.
- El backend completo mantiene skips documentados para escenarios que requieren MySQL/coverage driver no disponible en este host.
- Este gate no sustituye segunda PC LAN, impresora fisica ni tareas Windows elevadas.

## Actualizacion 2026-06-18

- `php artisan test`: PASS, 646 tests, 4273 assertions, 11 skipped.
- `vendor\bin\pint --test`: PASS.
- `vendor\bin\phpstan analyse --memory-limit=1G --no-progress`: PASS.
- `npm run test:full:windows`: PASS, 71 files, 324 tests.
- `npm run test:critical`: PASS, 7 files, 62 tests.
- `npm run typecheck`: PASS.
- `npm run lint`: PASS.
- `npm run build`: PASS.
- `npx playwright test e2e/production-readiness.spec.ts`: PASS, 3 tests.
- `scripts\assert_production_docker_sources.ps1`: PASS, `PRODUCTION_DOCKER_SOURCES: YES`.
- `scripts\validate_installer_safety.ps1`: PASS, sin hallazgos.
- `scripts\test_lan_deploy_hardening.ps1`: PASS.
- `scripts\assert_offline_release_clean.ps1`: PASS, `OFFLINE_RELEASE_CLEAN: YES`.
- `docker run --rm -v "${PWD}\backend:/app" -w /app composer:2 validate --strict`: PASS, `composer.json is valid`.
- `scripts\run_backend_tests_fast_mysql.ps1` contra MariaDB temporal Docker `mariadb:11.4.3`: PASS, golden DB creada/clonada y 7 tests focales ejecutados, 33 assertions.
- Reutilizacion de golden DB por hash contra la misma MariaDB temporal: PASS, segunda ejecucion reporto `Golden database already matches migration hash` y clono una DB disposable distinta.
- Se corrigio una referencia legacy de branding en scripts de tareas Windows construyendo el nombre de tareas anteriores en runtime, sin dejar la marca completa visible en scripts fuente ni en `offline-release\scripts`.
- Revalidacion posterior al ajuste: `validate_installer_safety.ps1`, `assert_offline_release_clean.ps1` y `test_lan_deploy_hardening.ps1` volvieron a pasar.
- Contenedor temporal de MariaDB golden eliminado; `docker ps -a --filter "name=s_hospital_golden"` no reporta contenedores pendientes.
- Hardening posterior de entrega: `init_production_proofs.ps1` ahora crea tambien plantillas para concurrencia bajo carga y smoke real LAN; `production_readiness_preflight.ps1` valida estrictamente `qa\FINAL_REAL_SMOKE_LAN_8081.md`; `assert_offline_release_clean.ps1` exige y compara solo plantillas `qa\*.example.md` dentro del paquete offline.
- Hardening RBAC: los permisos directos por modulo ahora requieren `users.assign_admin_role`; usuarios con solo `users.create/users.update` asignan rol operativo pero no pueden elevar permisos directos. Tambien se bloquean nombres reservados `admin/root` en roles editables y roles de guards distintos a `web`.
- Hardening golden DB: el hash de migraciones incluye `database\seeders\data\catalogo_servicios_inicial.csv` y la clonacion reactiva `FOREIGN_KEY_CHECKS` aunque falle antes de copiar datos.
- Runner golden DB protegido: `MonetaryCheckConstraintsTest` ya no ejecuta `migrate:fresh` cuando `HOSPITAL_TEST_DB_ALREADY_MIGRATED=1`; queda reservado para bases MySQL/MariaDB descartables fuera del flujo golden.
- Gates focales posteriores: `php artisan test --filter=UserManagementTest` PASS 17 tests/61 assertions; `RoleManagementTest` PASS 6/26; `MigrationHashTest` PASS 3/4; `GoldenDatabaseCommandTest` PASS 3/8; `npm run test -- UsersView.test.tsx --run` PASS 12 tests; `npm run typecheck` PASS; `npm run lint` PASS; `vendor\bin\pint --test` PASS; `vendor\bin\phpstan analyse --memory-limit=1G --no-progress` PASS; `scripts\test_backup_task_envfile_hardening.ps1` PASS; `scripts\assert_offline_release_clean.ps1` PASS.
- Gates amplios posteriores: `php artisan test` PASS, 650 passed, 11 skipped, 4286 assertions; `npm run test:critical` PASS, 7 files, 62 tests; `npm run build` PASS.
- Documentacion operativa alineada: `RELEASE_CHECKLIST`, `GUIA_INSTALACION_OPERATIVA`, `PENDIENTES_VALIDACION_CAMPO`, `PHASE_G_LAN_OFFLINE_VALIDATION_GUIDE`, `CI` y `DECISIONS` ya describen seis evidencias finales, no cuatro; `offline-release\docs` fue sincronizado y `assert_offline_release_clean.ps1` volvio a pasar.
- Gate de release reforzado: `.github\workflows\release.yml` ahora exige las seis evidencias finales, rechaza placeholders y valida `Decision: PRODUCTION_READY` en `qa\FINAL_PRODUCTION_HANDOFF_RESULT.md`.
- Automatizacion de evidencia reforzada: `scripts\auto_evidence.ps1` ahora prepara tambien `qa\FINAL_CONCURRENCY_UNDER_LOAD_PROOF_LAN_8081.md` y `qa\FINAL_REAL_SMOKE_LAN_8081.md`; `scripts\test_backup_task_envfile_hardening.ps1` cubre esta regresion.
- Guard offline reforzado: `scripts\assert_offline_release_clean.ps1` ahora exige y compara `scripts\auto_evidence.ps1` contra la fuente versionada.
- `scripts\auto_evidence.ps1 -Mode check` endurecido: ya no pasa solo por existencia de archivos; ahora valida placeholders, checks sin marcar y `Decision: PRODUCTION_READY`. En el estado actual devuelve `AUTO_EVIDENCE_READY: NO` correctamente porque falta impresion fisica en papel y el handoff sigue `READY_FOR_REAL_LAN_INSTALLATION_TEST`.
- `scripts\test_backup_task_envfile_hardening.ps1` ahora ejecuta `auto_evidence.ps1 -Mode check` contra fixtures temporales: evidencia incompleta debe fallar con `AUTO_EVIDENCE_READY: NO` y evidencia sintetica completa debe pasar con `AUTO_EVIDENCE_READY: YES`.
- Verificacion focal posterior: `scripts\test_backup_task_envfile_hardening.ps1` PASS; `scripts\assert_offline_release_clean.ps1` PASS; `scripts\auto_evidence.ps1 -Mode check` FAIL esperado por impresora/handoff no listos; `git diff --check` PASS para workflow, scripts y docs tocados.
- Portabilidad CI/PowerShell: `.github\workflows\ci.yml` ahora invoca `pwsh -NoProfile` en el runner Ubuntu; `scripts\test_backup_task_envfile_hardening.ps1` resuelve `pwsh`, `powershell.exe` o `powershell` antes de ejecutar los fixtures de `auto_evidence.ps1`.
- Revalidacion posterior de portabilidad: `scripts\test_backup_task_envfile_hardening.ps1` PASS; `scripts\assert_offline_release_clean.ps1` PASS.
- CI de scripts operativos: `.github\workflows\ci.yml` agrega job `windows-ops-scripts` en `windows-latest` para ejecutar `scripts\validate_installer_safety.ps1`, cubriendo instalador, tareas de backup y fixtures de `auto_evidence.ps1` en un entorno Windows real.
- Guard offline reforzado: `scripts\assert_offline_release_clean.ps1` ahora exige y compara `scripts\validate_installer_safety.ps1`; la copia `offline-release\scripts\validate_installer_safety.ps1` fue sincronizada.
- Revalidacion posterior del gate CI/instalador: `scripts\validate_installer_safety.ps1` PASS; `scripts\assert_offline_release_clean.ps1` PASS; `git diff --check` PASS para CI, docs y scripts tocados.
- Quality gate Windows reforzado: `scripts\quality_gate_windows.ps1` ahora ejecuta `ops scripts safety` con `scripts\validate_installer_safety.ps1` por defecto, con opcion `-SkipOps` solo para aislar diagnosticos.
- Guard offline reforzado: `scripts\assert_offline_release_clean.ps1` ahora exige y compara `scripts\quality_gate_windows.ps1`; la copia `offline-release\scripts\quality_gate_windows.ps1` fue sincronizada.
- Revalidacion posterior del quality gate local: `scripts\quality_gate_windows.ps1 -CriticalOnly -SkipBackend -SkipFrontend` PASS; `scripts\assert_offline_release_clean.ps1` PASS; `git diff --check` PASS para quality gate, docs y offline-release.
- Runner golden DB reforzado: `scripts\test_golden_db_runner_safety.ps1` agregado y conectado a `quality_gate_windows.ps1` y al job CI `windows-ops-scripts`; valida prefijos exactos `s_hospital_test_`/`s_hospital_golden_`, dry-run de hash, `HOSPITAL_TEST_DB_ALREADY_MIGRATED`, `phpunit.mysql.xml` y ausencia de resets destructivos.
- Seguridad golden DB reforzada: `testing:prepare-golden-database` ya no acepta nombres genericos que solo contengan `test/golden`; exige prefijo exacto para base disposable y golden. `TestCase` tambien rechaza DB externa sin prefijo `s_hospital_test_`.
- Hash golden DB reforzado: `MigrationHash::forLaravelBase` usa rutas relativas al backend y escanea `database\seeders` recursivamente, incluyendo seeders nuevos y archivos de datos sin depender de la ruta local del checkout.
- RBAC frontend reforzado: `UsersView` ahora muestra editar/restablecer clave solo con `users.update`, activar/desactivar solo con `users.disable`, y deja estado `Solo lectura` cuando el operador solo tiene `users.view`. La ruta de Reportes ahora permite `reports.managerial.view`, alineada con la navegacion.
- Revalidacion focal posterior: `scripts\test_golden_db_runner_safety.ps1` PASS; `GoldenDatabaseCommandTest` PASS 4 tests/12 assertions; `MigrationHashTest` PASS 5 tests/6 assertions; `UsersView.test.tsx` PASS 14 tests; `appNavigation.test.ts` PASS 3 tests; `npm.cmd run typecheck` PASS; `scripts\assert_offline_release_clean.ps1` PASS; `git diff --check` PASS para archivos tocados, con advertencias esperadas LF/CRLF en scripts PowerShell.
- Quality gate Windows posterior: `scripts\quality_gate_windows.ps1 -CriticalOnly -SkipFrontend` PASS. Incluyo `git diff check`, `ops scripts safety`, `golden db runner safety`, backend focal de eritropoyetina/caja/recibos/backups/autorizacion, `pint --test` y `phpstan analyse`.
- RBAC usuarios sin modulos reforzado: backend rechaza crear/editar/reactivar usuarios activos con mapa exacto de permisos vacio; usuarios inactivos pueden quedar preparados sin modulos hasta que la administradora asigne acceso.
- API de usuarios/auth reforzada: las respuestas incluyen `uses_exact_permission_map` para que la UI distinga entre herencia de rol y mapa exacto vacio sin exponer el marcador interno `system.exact_user_permissions`.
- Revalidacion focal posterior: `UserManagementTest` PASS 21 tests/75 assertions; `AuthTest` PASS 15 tests/40 assertions; `UsersView.test.tsx` PASS 16 tests; `npm.cmd run typecheck` PASS; `git diff --check` PASS para backend/frontend tocado.
- RBAC backups reforzado: `backups.restore` ahora existe en el seeder y en la matriz de permisos como permiso reservado; `BackupLogPolicy::restore` ya no apunta a un permiso imposible de sembrar. No se expone restore destructivo por UI ni endpoint web.
- Revalidacion focal posterior: `PermissionAuditTest` PASS 7 tests/30 assertions; `php -l backend\tests\Feature\PermissionAuditTest.php` PASS; guard estatico de permisos backend detecta checks no sembrados y solo exceptua `area_services.view` como legacy sin ruta activa.
- RBAC de roles custom reforzado: el editor de roles ya no expone ni acepta `users.assign_admin_role` para roles custom; el editor de usuarios rechaza roles operativos legacy que contengan ese permiso reservado.
- Permisos inoperables ocultos: `receipts.void` queda filtrado del catalogo/payloads visibles y rechazado en roles/usuarios, sin borrar el permiso de bases existentes.
- OpenAPI protegido: `/api/system/openapi` ahora exige `web`, `auth:web`, `user.active` y `password.changed`; usuarios con clave temporal reciben 403 antes de ver el mapa de API.
- Auditoria de operaciones protegida: `/api/reports/operations` exige `audit.view`; reportes gerenciales no dan acceso automatico a bitacora operacional.
- Frontend de nueva factura reforzado: navegacion y ruta usan permisos operativos completos; el CTA `Abrir Caja` se oculta cuando falta `cash.open` y muestra guia hacia caja/usuario autorizado.
- Frontend de historial reforzado: dialogos de anular, reversar y reimprimir bloquean confirmacion/cancelacion durante solicitud; la reversa tiene guard inmediato contra doble click.
- Revalidacion focal posterior: backend `php artisan test --filter="PermissionAuditTest|AuthTest|RoleManagementTest|UserManagementTest|OpenApiExporterTest|managerial_reports_without_close_any|operations_and_export_hide_backup_metadata|operations_report_lists"` PASS 67 tests/382 assertions. Frontend `npm.cmd test -- --run src/navigation/appNavigation.test.ts src/features/invoices/components/NewInvoiceViewLayout.test.tsx src/features/invoices/InvoiceHistoryView.test.tsx` PASS 15 tests. `npm.cmd run typecheck` PASS. `git diff --check` PASS para archivos tocados en esta tanda.
- Frontend de reportes reforzado: exportaciones ejecutivas y clasicas comparten guard de solicitud en curso para impedir doble click/doble descarga; los rangos ejecutivos invalidos bloquean refresco, PDF y Excel antes de llamar API.
- Revalidacion focal posterior: `npm.cmd test -- --run src/features/reports/ReportsView.test.tsx` PASS 15 tests; `npm.cmd test -- --run src/features/reports/components/CashSessionReportTab.test.tsx src/features/reports/components/AuditoriaTab.test.tsx src/features/reports/components/IncomeReportTab.test.tsx src/features/reports/components/ServiceSalesTab.test.tsx src/features/reports/components/MetricsGlossary.test.tsx` PASS 5 files/8 tests; `npm.cmd run typecheck` PASS; `git diff --check` PASS para reportes tocados.
- Frontend de respaldos reforzado: crear respaldo manual y descargar respaldo tienen guard inmediato en memoria contra doble click antes del re-render; los botones quedan deshabilitados mientras la solicitud sensible sigue pendiente.
- Revalidacion focal posterior: `npm.cmd test -- --run src/features/backups/BackupsView.test.tsx` PASS 1 file/2 tests; `npm.cmd run typecheck` PASS; `git diff --check` PASS para `BackupsView` y su test.
- Auditoria financiera reforzada: `payment.registered` ahora persiste `reference` en `audit_logs.new_values`, cerrando trazabilidad de transferencias/tarjetas contra conciliacion bancaria y recibos.
- Revalidacion focal posterior: `php artisan test --filter=CashPaymentsReceiptTest` PASS 28 tests/306 assertions; `php artisan test --filter=test_register_payment_creates_cash_movement_and_updates_partial_then_paid_invoice` PASS 1 test/18 assertions; `vendor\bin\pint --test app\Actions\Payments\RegisterPaymentAction.php tests\Feature\CashPaymentsReceiptTest.php` PASS; `git diff --check` PASS para archivos backend tocados.
- Facturacion UX/validacion reforzada: el frontend usa el mismo limite de 180 caracteres que backend para `patient_name`; el campo muestra contador accesible `actual/180`, anuncia errores junto con la ayuda y evita que el cajero llegue a un 422 tardio por longitud.
- Revalidacion focal posterior: backend `php artisan test --filter=InvoiceCreationTest` PASS 24 tests/103 assertions; frontend `npm.cmd test -- --run src/features/invoices/components/NewInvoiceViewLayout.test.tsx src/features/invoices/NewInvoiceView.test.tsx src/schemas/invoice.schema.test.ts src/features/invoices/components/PatientStep.test.tsx` PASS 4 files/25 tests; `npm.cmd run typecheck` PASS; `vendor\bin\pint --test tests\Feature\InvoiceCreationTest.php` PASS; `git diff --check` PASS para archivos de esta tanda.
- Preview de totales POS reforzado: `computeSimpleEstimate` ahora redondea subtotales fraccionarios con la misma regla half-up del backend y calcula ISV sobre el subtotal gravado agregado, reduciendo diferencias visuales antes de emitir la factura.
- Revalidacion focal posterior: `npm.cmd test -- --run src/features/invoices/state/posMath.test.ts` PASS 1 file/12 tests; `npm.cmd test -- --run src/features/invoices/NewInvoiceView.test.tsx src/features/invoices/components/InvoiceCart.test.tsx src/features/invoices/state/posMath.test.ts` PASS 3 files/36 tests; backend `php artisan test --filter=test_cashier_can_create_invoice_with_multiple_services_and_backend_totals` PASS 1 test/15 assertions; `npm.cmd run typecheck` PASS; `git diff --check` PASS para `posMath`.
- Recibos media carta reforzados: `ReceiptPreview` ahora emite la clase CSS imprimible `receipt-half-letter` en lugar de `receipt-half_letter`; los conceptos largos pueden partir palabra dentro de la columna de servicio sin invadir el valor, y el recibo imprimible queda cubierto contra QR/codigo de barras/codigos internos visibles.
- Revalidacion focal posterior: `npm.cmd test -- --run src/features/receipts/ReceiptPreview.test.tsx src/features/receipts/ReceiptPreview.a11y.test.tsx src/features/invoices/NewInvoiceView.test.tsx` PASS 3 files/30 tests; `npm.cmd run typecheck` PASS; `git diff --check` PASS para `ReceiptPreview`, pruebas de recibos/factura y `styles.css`.
- Cobertura operativa de caja reforzada: se agregaron pruebas explicitas para rechazar pagos parciales cuando la configuracion los deshabilita, impedir cerrar una caja ya cerrada y bloquear que un cajero cierre la caja de otro usuario. Las pruebas verifican ausencia de pagos/movimientos indebidos y conservacion de estados.
- Revalidacion focal posterior: `php artisan test --filter=CashPaymentsReceiptTest` PASS 31 tests/326 assertions; `vendor\bin\pint --test tests\Feature\CashPaymentsReceiptTest.php` PASS; `git diff --check` PASS para `CashPaymentsReceiptTest`.
- Accesibilidad de Historial revisada: el hallazgo A11Y-04 del registro operativo ya no aplica al codigo actual porque las acciones de fila son botones nativos visibles y no existe overlay `div` clicable ni estado `openActionsId` en `InvoiceHistoryView`.
- Revalidacion focal posterior: `npm.cmd run lint` PASS sin warnings; `rg '<div[^>]*onClick' frontend/src/features frontend/src/components` sin resultados; busqueda de `role="button"|tabIndex|fixed inset-0` solo encontro overlays de Radix/dialogos y controles con teclado explicito en busqueda/seleccion.
- Seguridad auth reforzada por cobertura: SEC-AUD-001 queda verificado con pruebas de auditoria forense para `auth.login`, `auth.login_failed`, `auth.login_success`, `auth.password_changed` y `auth.logout`, confirmando que los payloads no guardan `password` ni `current_password`.
- Revalidacion focal posterior: `php artisan test --filter=AuthTest` PASS 17 tests/62 assertions; `vendor\bin\pint --test tests\Feature\AuthTest.php` PASS; inventario `qa\SECURITY_FINDINGS.md` y anexo `qa\SECURITY_V1_1_DEFERRED.md` actualizados para no mantener SEC-AUD-001 como diferido.
- Seguridad de administracion de usuarios reforzada por cobertura: SEC-AUD-004 queda verificado para `user.created`, `user.updated`, `user.deactivated` y `user.password_reset`; reset de contrasena confirma que `password` no se guarda en `old_values` ni `new_values`.
- Revalidacion focal posterior: `php artisan test --filter=UserManagementTest` PASS 23 tests/86 assertions; `php artisan test --filter=InternalControlAuditTest` PASS 7 tests/53 assertions; `vendor\bin\pint --test tests\Feature\UserManagementTest.php` PASS; `qa\SECURITY_FINDINGS.md` y `qa\SECURITY_V1_1_DEFERRED.md` actualizados para no mantener SEC-AUD-004 como diferido.
- Policies backend invocadas en controllers: `CashSessionController::close` ahora llama `Gate::authorize('close', $cashSession)` y `InvoiceController::void/reverse` llama `Gate::authorize('void'/'reverse', $invoice)`. La policy de caja autoriza identidad/permiso y deja el estado cerrado a `CloseCashSessionAction`, preservando el 422 operativo "La caja ya esta cerrada".
- Revalidacion focal posterior: primera corrida de `CashPaymentsReceiptTest` detecto regresion 403 vs 422 en doble cierre; se corrigio `CashSessionPolicy::close`. Reintento `php artisan test --filter=CashPaymentsReceiptTest` PASS 31 tests/326 assertions; `php artisan test --filter=InvoiceHistoryReprintVoidTest` PASS 17 tests/116 assertions; `php artisan test --filter=InvoiceReverseTest` PASS 8 tests/45 assertions; `php artisan test --filter=PermissionAuditTest` PASS 7 tests/30 assertions; `vendor\bin\pint --test app\Http\Controllers\CashSessionController.php app\Http\Controllers\InvoiceController.php app\Policies\CashSessionPolicy.php` PASS.
- Auditoria de lectura de recibos reforzada: `GET /api/invoices/{invoice}/receipt` ahora registra `receipt.viewed` con factura, estado y ancho solicitado, sin mutar datos financieros ni contar como reimpresion formal. `invoice.reprinted` sigue reservado para reimpresion con motivo.
- Revalidacion focal posterior: `php artisan test --filter=CashPaymentsReceiptTest` PASS 32 tests/340 assertions; `vendor\bin\pint --test app\Http\Controllers\ReceiptController.php tests\Feature\CashPaymentsReceiptTest.php` PASS; `qa\SECURITY_FINDINGS.md` y `qa\SECURITY_V1_1_DEFERRED.md` actualizados para cerrar SEC-AUD-005.
- Auditoria de receta de dialisis reforzada: `CreateInvoiceAction` ahora registra `invoice.dialysis_prescription_applied` solo cuando la regla de Eritropoyetina realmente se aplica a una factura creada, y registra `invoice.dialysis_prescription_denied` fuera de la transaccion revertida cuando un usuario sin permiso intenta activar el toggle.
- Revalidacion focal posterior: `php artisan test --filter=InvoiceDialysisPrescriptionTest` PASS 5 tests/34 assertions; `vendor\bin\pint app\Actions\Billing\CreateInvoiceAction.php tests\Feature\Billing\InvoiceDialysisPrescriptionTest.php` ejecutado para normalizar cambios; `qa\SECURITY_FINDINGS.md` y `qa\SECURITY_V1_1_DEFERRED.md` actualizados para cerrar SEC-AUD-006.
- Auth con password temporal confirmado por contrato: usuarios con `must_change_password=true` pueden consultar `/api/auth/me`, cambiar password o cerrar sesion; rutas operativas siguen bloqueadas con 403 y el logout queda en `audit_logs`.
- Revalidacion focal posterior: `php artisan test --filter=AuthTest` PASS 17 tests/65 assertions; `vendor\bin\pint --test tests\Feature\AuthTest.php` PASS; `git diff --check` PASS para `AuthTest` e inventarios de seguridad. `SEC-AUTH-034` queda `FIXED/DESIGN-CONFIRMED`.
- Control interno de usuarios reforzado: el reset administrativo de contrasena ahora usa `UserPolicy::resetPassword` con usuario objetivo y deniega self-reset aunque el actor tenga `users.update`; no se muta password ni se escribe `user.password_reset` en intentos propios.
- Revalidacion focal posterior: `php artisan test --filter=UserManagementTest` PASS 24 tests/89 assertions; `php artisan test --filter=PermissionAuditTest` PASS 7 tests/31 assertions; `vendor\bin\pint` aplicado al bloque de policy/request/tests; `SEC-AUTH-010` queda FIXED.
- Politica de contrasenas reforzada para produccion offline: backend, comando `auth:create-initial-admin`, UI de usuarios y pantalla de cambio obligatorio exigen minimo 12 caracteres con mayuscula, minuscula, numero y simbolo. No se usa chequeo `uncompromised` porque dependeria de internet.
- Revalidacion focal posterior: backend `AuthTest` PASS 17 tests/65 assertions; `UserManagementTest` PASS 24 tests/89 assertions; `InitialAdminCommandTest` PASS 5 tests/11 assertions; `RoleManagementTest` PASS 8 tests/35 assertions; `InternalControlAuditTest` PASS 7 tests/53 assertions; `ValidationSecurityTest` PASS 5 tests/10 assertions. Frontend `npm.cmd test -- --run src/features/admin/UsersView.test.tsx src/App.test.tsx` PASS 2 files/33 tests; `npm.cmd run typecheck` PASS; `vendor\bin\pint --test` PASS; `git diff --check` PASS para archivos tocados. `SEC-AUTH-008` queda FIXED.
- Auth de usuarios inactivos reforzado: `AuthController::login` bloquea usuarios desactivados antes de `Auth::attempt`, mantiene `login_attempts.success=false`, no deja sesion autenticada y registra `auth.login_blocked`.
- Revalidacion focal posterior: `php artisan test --filter=AuthTest` PASS 18 tests/72 assertions; `vendor\bin\pint --test app\Http\Controllers\AuthController.php tests\Feature\AuthTest.php` PASS; `git diff --check` PASS para auth. `SEC-AUTH-021` queda FIXED.
- Validacion de roles en administracion cerrada: crear/editar usuarios exige roles existentes del guard `web`; rol desconocido y roles de otros guards se rechazan antes de que Spatie pueda crear/usar nombres arbitrarios.
- Revalidacion focal posterior: `php artisan test --filter=UserManagementTest` PASS 25 tests/93 assertions; `vendor\bin\pint` aplicado a requests/tests del bloque; `SEC-AUTH-011` queda FIXED.
- Payload de detalle de factura reducido: `InvoiceController::show` ya no carga ni serializa la relacion `fiscal_sequence`; se mantienen los snapshots propios de la factura (`fiscal_cai`, rango, vigencia) para recibos y auditoria historica.
- Revalidacion focal posterior: `php artisan test --filter=InvoiceHistoryReprintVoidTest` PASS 17 tests/117 assertions; `php artisan test --filter=InvoiceReverseTest` PASS 8 tests/45 assertions; `vendor\bin\pint --test app\Http\Controllers\InvoiceController.php tests\Feature\InvoiceHistoryReprintVoidTest.php` PASS; `git diff --check` PASS. `SEC-ADV-027` queda FIXED.
- Tiempo de sesion por defecto endurecido: `config/session.php`, `.env.example` y `docker-compose.prod.yml` quedan alineados a `SESSION_LIFETIME=60` para reducir ventanas de sesion abandonada en caja LAN.
- Revalidacion focal posterior: `php artisan test --filter=SessionSecurityConfigTest` PASS 1 test/2 assertions; `vendor\bin\pint --test config\session.php tests\Unit\SessionSecurityConfigTest.php` PASS; busqueda de referencias `SESSION_LIFETIME.*120` y `env('SESSION_LIFETIME', 120)` sin resultados; `git diff --check` PASS. `SEC-AUTH-017` queda FIXED.
- CSRF frontend reforzado por cobertura: `apiClient` mantiene TTL de cache XSRF en 10 minutos y una prueba de frontera impide volver accidentalmente al cache de 30 minutos que dejaba tokens obsoletos reutilizables.
- Revalidacion focal posterior: `npm.cmd test -- --run src/lib/api/base.test.ts` PASS 31 tests; `npm.cmd run typecheck` PASS; busqueda de `30-minute cache` y `CSRF_CACHE_TTL_MS = 30` sin resultados en frontend; `git diff --check` PASS. `SEC-AUTH-036` queda FIXED.
- OpenAPI protegido y documentacion QA alineada: `/api/system/openapi` queda fuera de rutas publicas, exige usuario autenticado/activo/con password cambiada y mantiene `throttle.user:30,1`; la matriz API ya no conserva la excepcion obsoleta.
- Revalidacion focal posterior: `php artisan test --filter=OpenApiExporterTest` PASS 11 tests/114 assertions; `vendor\bin\pint --test tests\Feature\OpenApiExporterTest.php` PASS; busqueda de `route still public` y `only exception is` sin resultados; `git diff --check` PASS. `SEC-API-001` y `SEC-ADV-001` quedan FIXED.
- Rangos de reportes endurecidos: requests clasicos y ejecutivos ya no usan `9999-12-31` como fallback cuando `date_from` es invalido; caen a `hoy + rango permitido` y rechazan `date_to` futuro excesivo aun cuando el inicio venga malformado.
- Revalidacion focal posterior: `php artisan test --filter='test_income_report_respects_date_range_and_invalid_ranges_return_422|test_period_closure_pdf_export_validates_range_filters|test_executive_endpoint_validates_date_range|test_executive_pdf_rejects_far_future_date_to_when_date_from_is_malformed'` PASS 4 tests/57 assertions; `vendor\bin\pint --test` PASS sobre requests/tests tocados; busqueda de `9999-12-31` sin resultados en requests de reportes; `git diff --check` PASS. `SEC-API-014` queda FIXED.
- Privacidad de logs nginx reforzada por guard: `location /api/` mantiene `access_log off;` para no persistir query strings con busquedas de paciente (`?patient=...`) en logs del proxy.
- Revalidacion focal posterior: `php artisan test --filter=NginxPrivacyConfigTest` PASS 1 test/2 assertions; `vendor\bin\pint --test tests\Unit\NginxPrivacyConfigTest.php` PASS; `rg` confirma `access_log off` en `nginx/default.conf`; `git diff --check` PASS. `SEC-API-011` queda FIXED.
- Templates de entorno sin debug: `.env.example`, `backend/.env.example`, `backend/.env.docker.example` y `docker-compose.prod.yml` no entregan `APP_DEBUG=true`; produccion mantiene `APP_DEBUG=false` por defecto.
- Revalidacion focal posterior: `php artisan test --filter=ProductionEnvTemplateTest` PASS 1 test/12 assertions; `vendor\bin\pint --test tests\Unit\ProductionEnvTemplateTest.php` PASS; busqueda de `APP_DEBUG=true` en templates/Compose sin resultados; `git diff --check` PASS. `SEC-API-019` queda FIXED.
- Secretos Windows/devex reforzados: `devex/docker-compose.example.yml` exige placeholders de secretos y `setup.bat` usa `System.Security.Cryptography.RandomNumberGenerator` en vez de `Get-Random` para credenciales locales; `deploy_hospital_lan.ps1` ya estaba alineado.
- Revalidacion focal posterior: `php artisan test --filter=WindowsInstallSecretsTest` PASS 2 tests/11 assertions; `vendor\bin\pint --test tests\Unit\WindowsInstallSecretsTest.php` PASS; busqueda de `hospital_dev`, `root_dev` y `Get-Random` en `devex/docker-compose.example.yml`, `setup.bat` y `scripts/deploy_hospital_lan.ps1` sin resultados; `git diff --check` PASS con advertencias LF/CRLF esperadas. `SEC-SEC-004` y `SEC-SEC-005` quedan FIXED.
- Auditoria de permisos observable ante fallo: `PermissionAuditObserver` sigue sin romper cambios de permisos si `audit_logs` falla, pero ahora emite `Log::warning`, guarda `permission_audit_observer:last_failure` y `/api/system/health` lo reporta como `permission_audit_observer_failed`.
- Revalidacion focal posterior: `php artisan test --filter=PermissionAuditTest` PASS 8 tests/38 assertions; `php artisan test --filter=HealthCheckTest` PASS 7 tests/39 assertions; `vendor\bin\pint --test` PASS para observer/metrics/test; `git diff --check` PASS. `SEC-AUD-013` queda FIXED.
- Privacidad de reporte operativo minimizada: `OperationsReportService` ya no devuelve `patient_name` en anulaciones/reversos ni `username` en cajeros; los eager-loads del reporte operativo piden solo `id,name` cuando basta el nombre visible. `AuditoriaTab` y los contratos TypeScript ya no esperan ni muestran esos campos, y el Excel operativo hereda la misma fuente minimizada.
- Revalidacion focal posterior: `php artisan test --filter='operations_report_area_filter_prorates_cashier_totals_from_invoice_item_snapshots|operations_report_uses_payment_amount_cents_as_financial_source|range_filters_apply_to_category_services_and_cashier_reports|operations_report_lists_voids_reprints_and_backups|operations_report_lists_payment_reversals'` PASS 5 tests/113 assertions; `npm.cmd test -- --run src/features/reports/components/AuditoriaTab.test.tsx src/features/reports/ReportsView.test.tsx` PASS 2 files/17 tests; `npm.cmd run typecheck` PASS; `vendor\bin\pint --test app\Actions\Reports\OperationsReportService.php tests\Feature\ReportsTest.php` PASS; `git diff --check` PASS. `SEC-PRIV-005` y `SEC-PRIV-006` quedan FIXED.
- CI sin credenciales estaticas: `.github/workflows/ci.yml` mantiene `CI_MARIADB_PASSWORD`/`CI_MARIADB_ROOT_PASSWORD` desde secrets o vars, pero el fallback ahora se deriva de `github.run_id` para evitar credenciales fijas en el repositorio.
- Revalidacion focal posterior: `php artisan test --filter=WindowsInstallSecretsTest` PASS 3 tests/16 assertions; `vendor\bin\pint --test tests\Unit\WindowsInstallSecretsTest.php` PASS; busqueda de los fallbacks antiguos `ci-db-only-change-in-repo-settings` y `ci-root-db-only-change-in-repo-settings` sin resultados fuera de asserts negativos; `git diff --check` PASS. `SEC-SEC-007` queda FIXED.
- Sanitizacion de logs legacy reforzada: `OperationalMessageSanitizer` y `collect_support_packet.ps1` redaccionan trazas MySQL `hospital_app@IP` y credenciales embebidas en URL antes de exponer mensajes o tails de `laravel.log`. No se modifican ni borran logs reales.
- Revalidacion focal posterior: `php artisan test --filter=OperationalMessageSanitizerTest` PASS 2 tests/6 assertions; `powershell -NoProfile -ExecutionPolicy Bypass -File scripts\validate_support_packet_safety.ps1` PASS; `vendor\bin\pint --test app\Support\OperationalMessageSanitizer.php tests\Unit\OperationalMessageSanitizerTest.php` PASS; `git diff --check` PASS con advertencias LF/CRLF esperadas en scripts PowerShell. `SEC-SEC-008` queda FIXED.
- Privacidad de capturas QA reforzada: nuevos artefactos `qa/screenshots/**/*.png|jpg|jpeg|webp|json` y `qa/financial-data-audit/screenshots/` quedan ignorados por defecto; `frontend/e2e/rc1-screens.spec.ts` ahora escribe capturas en `frontend/test-results/rc1-screens` salvo que `E2E_CAPTURE_SCREENS_DIR` se configure explicitamente.
- Revalidacion focal posterior: `php artisan test --filter=QaScreenshotPrivacyGuardTest` PASS 2 tests/11 assertions; `npm.cmd run typecheck` PASS; `vendor\bin\pint --test tests\Unit\QaScreenshotPrivacyGuardTest.php` PASS; `git diff --check` PASS. `SEC-PRIV-012` queda FIXED.
- Preflight de privacidad de exportaciones reforzado: `production_readiness_preflight.ps1` ejecuta `Test-ReportExportPrivacyGuards`, que valida que `OperationsReportService` no vuelva a serializar `patient_name`/`username` y que `ReportsTest` conserve introspeccion XLSX con `IOFactory::load($path)` y asserts negativos para campos sensibles.
- Revalidacion focal posterior: `powershell -NoProfile -ExecutionPolicy Bypass -File scripts\test_backup_task_envfile_hardening.ps1` PASS; `git diff --check` PASS con advertencia LF/CRLF esperada. `SEC-PRIV-022` queda FIXED.
- Respuestas 500 de API saneadas: cuando `APP_DEBUG=false`, el renderizador de excepciones de Laravel responde JSON 5xx pasando el mensaje por `OperationalMessageSanitizer` y sin exponer rutas locales, usuario/host de DB ni secretos.
- Revalidacion focal posterior: `php artisan test --filter=ApiExceptionRenderingTest` PASS 1 test/9 assertions; `vendor\bin\pint --test bootstrap\app.php tests\Feature\ApiExceptionRenderingTest.php` PASS; `git diff --check` PASS. `SEC-PRIV-025` queda FIXED.
- Golden DB para tests rapidos validada: `testing:prepare-golden-database` usa hash de migraciones y seeders, prefijos estrictos `s_hospital_test_`/`s_hospital_golden_`, metadata `_test_golden_metadata`, y `TestCase` respeta `HOSPITAL_TEST_DB_ALREADY_MIGRATED=1` para no repetir migraciones sobre clones preparados.
- Revalidacion focal posterior: `powershell -NoProfile -ExecutionPolicy Bypass -File scripts\test_golden_db_runner_safety.ps1` PASS; `php artisan test --filter='GoldenDatabaseCommandTest|MigrationHashTest'` PASS 9 tests/18 assertions.
- RBAC configurable por administradora validado: backend permite roles custom y permisos directos por usuario, oculta permisos internos/reservados, bloquea usuarios activos sin modulos y conserva roles base protegidos. Frontend consume catalogo de permisos/roles desde API y no hardcodea solo roles base.
- Revalidacion focal posterior: `php artisan test --filter='RoleManagementTest|UserManagementTest|PermissionAuditTest'` PASS 41 tests/166 assertions; `npm.cmd test -- --run src/features/admin/UsersView.test.tsx src/navigation/appNavigation.test.ts` PASS 2 files/19 tests.
- Preflight productivo corregido: `scripts\production_readiness_preflight.ps1` ya no expande `$path` dentro del regex `IOFactory::load\(\$path\)` y el guard de privacidad de reportes corre correctamente.
- Refresh LAN productivo reforzado: `scripts\refresh_lan_ip.ps1` acepta `-EnvFile` externo y `-ComposeProjectName`, sincroniza `APP_URL`, `SANCTUM_STATEFUL_DOMAINS`, `CORS_ALLOWED_ORIGINS` y `PUSHER_CLIENT_HOST`, y recrea contenedores con el env efectivo sin borrar volumenes.
- Revalidacion focal posterior: `scripts\test_lan_deploy_hardening.ps1` PASS; `scripts\test_backup_task_envfile_hardening.ps1` PASS; `refresh_lan_ip.ps1 -ServerIp 192.168.1.10 -AppPort 8081 -EnvFile C:\tmp\s_hospital_offlinetest.env -ComposeProjectName shospital_offlinetest -WhatIf` PASS sin aplicar cambios; `git diff --check` PASS para scripts/docs tocados con advertencias LF/CRLF esperadas.
- Preflight real posterior contra `http://192.168.1.10:8081`: ejecuta y confirma que el servidor responde `/up`, `/login` y `/verify-email`, pero `PRODUCTION_READY` sigue NO porque el env final aun apunta a `192.168.1.7`, faltan tareas Windows elevadas/current-user fallback y se uso `-AllowMissingPhysicalProof`.
- Refresh LAN aplicado: `scripts\refresh_lan_ip.ps1 -ServerIp 192.168.1.10 -AppPort 8081 -EnvFile C:\tmp\s_hospital_offlinetest.env -ComposeProjectName shospital_offlinetest` termino PASS, actualizo env efectivo y recreo backend, queue-worker, scheduler y nginx sin borrar volumenes.
- Verificacion posterior: `docker compose -p shospital_offlinetest -f docker-compose.prod.yml --env-file C:\tmp\s_hospital_offlinetest.env ps` muestra backend/mysql/nginx/queue-worker/scheduler/soketi healthy; `/up` responde 200; `/api/system/echo-config` responde 200 con `host=192.168.1.10`.
- Fallback de backups usuario actual instalado: `scripts\install_backup_startup_current_user.ps1 -Mode Docker -EnvFile C:\tmp\s_hospital_offlinetest.env -ComposeProjectName shospital_offlinetest -DailyBackupTime 02:00` PASS; `-Status` confirma Startup/HKCU instalados.
- Instalacion elevada de tareas Windows solicitada: `scripts\install_backup_tasks_windows.ps1 -Mode Docker -EnvFile C:\tmp\s_hospital_offlinetest.env -ComposeProjectName shospital_offlinetest -UpdateExisting -LaunchElevated` PASS; `qa\WINDOWS_BACKUP_TASK_ELEVATED_INSTALL.log` registra `Scheduled tasks registered successfully` y tareas `SistemaCajaHospitalaria-*` `state=Ready` como `SYSTEM`.
- Preflight posterior contra `http://192.168.1.10:8081`: APP_URL, Sanctum, CORS, Pusher host/port, Soketi bind, Docker backup wrappers, rutas `/up`/`/login`/`/verify-email` y fallback Startup/HKCU pasan. `PRODUCTION_READY` sigue NO por verificacion de tareas SYSTEM no legible desde sesion no elevada y por prueba fisica omitida con `-AllowMissingPhysicalProof`.
- Preflight endurecido para evidencias finales: acepta el log elevado reciente de tareas Windows solo si el ultimo intento tiene registro exitoso, ambas tareas `Ready` como `SYSTEM` y ningun `ERROR` posterior. Tambien exige que `qa\LAN_CLIENT_VALIDATION_PROOF.md` referencie la `BaseUrl` final actual.
- Preflight final sin `-AllowMissingPhysicalProof` contra `http://192.168.1.10:8081`: tareas Windows elevadas aceptadas por evidencia, restore/concurrencia/carga/smoke real pasan, pero `PRODUCTION_READY: NO (2 blocking issue(s))` porque falta repetir segunda PC LAN contra `192.168.1.10` y falta impresion fisica media carta/papel real.
